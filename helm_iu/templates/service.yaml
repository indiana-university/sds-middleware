apiVersion: v1
kind: Service
metadata:
  name: {{ include "sds-middleware.fullname" . }}
  namespace: {{ include "sds-middleware.namespace" . }}
  labels:
    {{- include "sds-middleware.labels" . | nindent 4 }}
spec:
  type: {{ .Values.sdsMiddleware.service.type }}
  selector:
    {{- include "sds-middleware.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.sdsMiddleware.service.port }}
      targetPort: http
      protocol: TCP
