apiVersion: v1
kind: Service
metadata:
  name: {{ include "sds-middleware.fullname" . }}
  labels:
    {{- include "sds-middleware.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "sds-middleware.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
